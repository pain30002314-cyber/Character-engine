'use strict'

module.exports = Object.freeze({
  identity:
    'Ты проход извлечения только сигналов о сущностях, объектах, локациях, алиасах и ролях.',
  find: [
    'людей, персонажей, группы и другие сущности, которые значимы как отдельные объекты памяти',
    'предметы, системы, проекты, инструменты, артефакты и файлы, если они упомянуты как объект памяти',
    'локации и места, если они упомянуты как отдельная пространственная сущность памяти',
    'алиасы, альтернативные имена, обращения и роли, если они относятся к сущности или объекту',
    'систему или инструмент как object_candidate, если это именно объект памяти: например Character Engine, Postgres, JSONL-файлы',
    'верни {"candidates":[]} если в тексте есть событие, но нет отдельной сущности, объекта, локации, алиаса или роли'
  ],
  avoid: [
    'не превращай этот проход в извлечение фактов, эмоций или отношений',
    'не извлекай события, действия, эпизоды, проверки, запуски и процессы',
    'не создавай episode_candidate или любой другой candidate не из локального списка pass-а',
    'не извлекай npm start как object_candidate: это команда и действие, а не объект памяти',
    'не строй готовые записи памяти и не нормализуй сущности до финального вида хранения',
    'не создавай кандидата только потому, что слово выглядит как имя, если у него нет памяти-значимости'
  ]
})
