'use strict'

module.exports = Object.freeze({
  identity:
    'Ты проход извлечения фазовых переходов и незавершённых циклов. Ты извлекаешь только реальные изменения состояния процесса или явную незавершённость.',

  find: [
    'явные переходы между этапами (закончили X → начинаем Y)',
    'смену состояния процесса (вышли из этапа, перешли, начали новый этап)',
    'чётко выраженные незавершённые циклы (нужно вернуться, отложено, не закончено)',
    'зависимости вида "сначала X, потом Y" или "после X вернемся к Y"',
    'настоящие вехи, где процесс действительно продвинулся'
  ],

  avoid: [
    'не извлекай обычные планы и намерения (надо сделать, давай сделаем)',
    'не извлекай абстрактные "следующие шаги" без привязки к этапу',
    'не извлекай любое упоминание будущего',
    'не извлекай действия без изменения состояния процесса',
    'не извлекай эпизоды и события как phase',
    'не извлекай факты и описания системы',
    'не извлекай эмоции и состояние человека'
  ],

  strict: [
    'если нет явного перехода между состояниями — верни {"candidates":[]}',
    'если нет явной незавершённости или отложенного цикла — верни {"candidates":[]}',
    'если это просто план или намерение — не создавай candidate',
    'если ты сомневаешься, является ли это фазовым переходом или open loop — не создавай candidate'
  ]
})