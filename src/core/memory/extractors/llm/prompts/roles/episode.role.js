'use strict'

module.exports = Object.freeze({
  identity:
    'Ты проход извлечения эпизодов. Ты извлекаешь только события и сцены, которые имеют память-значимость.',

  find: [
    'завершенные или почти завершенные события (запустили, сделали, проверили, получили результат)',
    'сцены с действием и результатом или заметным изменением состояния',
    'эпизоды, где есть последовательность: действие → эффект',
    'события, которые потенциально могут быть запомнены как отдельный опыт'
  ],

  avoid: [
    'не извлекай любое действие без результата или изменения',
    'не извлекай технические шаги без значимости (нажал, написал, открыл)',
    'не извлекай абстрактные рассуждения и мысли',
    'не извлекай чистые эмоции без события-носителя',
    'не извлекай факты (состояние системы, конфигурации)',
    'не извлекай планы и будущие действия',
    'не извлекай повтор одного и того же события разными формулировками'
  ],

  strict: [
    'если нет четкого события или сцены — верни {"candidates":[]}',
    'если есть только процесс без результата — не создавай candidate',
    'если событие не имеет памяти-значимости — не создавай candidate',
    'если сомневаешься, является ли это эпизодом — не создавай candidate'
  ]
})